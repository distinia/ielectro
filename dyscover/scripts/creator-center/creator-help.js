export const CreatorHelp = {
    article: `
        <p><strong>Create a text article</strong> with metadata before writing the full content.</p>
        <ul>
            <li>Set a clear title and a short description for feeds and search.</li>
            <li>Type <code>#</code> to add tags (maximum 5). Existing tags autocomplete from the server.</li>
            <li>After creating, open the article editor to write the full text.</li>
        </ul>
        <p>You can edit title, description and tags here anytime.</p>`,
    image: `
        <p><strong>Upload an image post</strong> with title, description and tags.</p>
        <ul>
            <li>Type <code>#</code> to add tags (maximum 5). Existing tags autocomplete from the server.</li>
            <li>Supported formats: JPG, PNG, WebP.</li>
            <li>Maximum file size: 10 MB.</li>
            <li>Use <strong>Choose file</strong> in the footer, then <strong>Upload</strong>.</li>
            <li>The preview on the right updates when you pick a file.</li>
        </ul>
        <p>When editing, leave the file empty to keep the current image.</p>`,
    video: `
        <p><strong>Upload a video post</strong> with metadata and a media file.</p>
        <ul>
            <li>Type <code>#</code> to add tags (maximum 5). Existing tags autocomplete from the server.</li>
            <li>Supported formats include MP4 and WebM.</li>
            <li>Maximum file size: 250 MB.</li>
            <li>Use <strong>Choose file</strong> in the footer, then <strong>Upload</strong>.</li>
            <li>Large files may take longer to upload and process.</li>
        </ul>
        <p>Use the preview panel to check the selected video before uploading.</p>`,
    audio: `
        <p><strong>Upload an audio post</strong> with metadata and a media file.</p>
        <ul>
            <li>Type <code>#</code> to add tags (maximum 5). Existing tags autocomplete from the server.</li>
            <li>Supported formats include MP3 and WAV.</li>
            <li>Maximum file size: 250 MB.</li>
            <li>Use <strong>Choose file</strong> in the footer, then <strong>Upload</strong>.</li>
            <li>Fill in title and tags so listeners can find your track.</li>
        </ul>
        <p>When editing, leave the file empty to keep the current audio.</p>`,
    document: `
        <p><strong>Upload a PDF document</strong> with title, description and tags.</p>
        <ul>
            <li>Type <code>#</code> to add tags (maximum 5). Existing tags autocomplete from the server.</li>
            <li>Only PDF files are accepted.</li>
            <li>Maximum file size: 50 MB.</li>
            <li>Use <strong>Choose file</strong> in the footer, then <strong>Upload</strong>.</li>
            <li>The preview panel shows the selected document when possible.</li>
        </ul>
        <p>When editing, leave the file empty to keep the current document.</p>`,
    template: `
        <p><strong>Build a reusable template</strong> with fixed fields other people can fill in.</p>
        <ul>
            <li>Type <code>#</code> to add tags (maximum 5). Existing tags autocomplete from the server.</li>
            <li>Add a title, description and tags on the left.</li>
            <li>Upload a preview image shown in feeds and search.</li>
            <li>Create fields on the right: name each block and pick its type.</li>
            <li>Reorder by removing and re-adding fields if needed.</li>
        </ul>
        <p>When you publish, users can create posts from this structure.</p>`,
};
